import styles from './app.module.css';

const App = () => {
    return (
        <div data-lego-app-wrapper className={styles.app}>
            <h1>Embeddable App</h1>
        </div>
    );
};

export default App;
